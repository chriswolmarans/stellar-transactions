import { DecimalValidationDirectiveDirective } from './decimal-validation-directive.directive';

describe('DecimalValidationDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalValidationDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
